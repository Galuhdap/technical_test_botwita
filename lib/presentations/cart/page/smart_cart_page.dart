import 'package:flutter/material.dart';

class SmartCartPage extends StatelessWidget {
  const SmartCartPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Smart Cart Page')));
  }
}
